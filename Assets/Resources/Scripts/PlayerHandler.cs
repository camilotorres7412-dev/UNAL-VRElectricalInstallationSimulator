using UnityEngine;

public class PlayerHandler : MonoBehaviour
{
    // Stores the fixture held in hand
    public GameObject heldFixture;

    public void SetFixture(GameObject gameObject)
    {
        heldFixture = gameObject;
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
